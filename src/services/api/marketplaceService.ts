import { supabase } from '../supabaseClient';
import { Business, FleetUnit, Driver, Administrator, Destination, Trip, PricingSettings, KPILog, MarketplaceListing, Product, CommercialTransaction, Lead } from './types';

// --- Marketplace (Comercializadora) ---
